# The profiler doesn't need LLM prompts -- it's deterministic (toolkit.profile()).
# This file exists for consistency with the file structure pattern.
# If we later need LLM-assisted profiling (e.g., for column type suggestions),
# the prompt would go here.
